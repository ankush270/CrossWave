import { Router } from "express";
// import { getProductReviews, getUserReviews, productAvgRating, productReview, userAvgRating, userReview } from "../controllers/review.controller.js";

const router = Router();

// router.route('/user-review/:reviewee_id').post(userReview);
// router.route('/get-user-reviews/:reviewee_id').get(getUserReviews);
// router.route('/user-avg-rating/:reviewee_id').get(userAvgRating);
// router.route('/product-review/:product_id').post(productReview);
// router.route('/get-product-reviews/:product_id').get(getProductReviews);
// router.route('/product-avg-rating/:product_id').get(productAvgRating);

export default router;